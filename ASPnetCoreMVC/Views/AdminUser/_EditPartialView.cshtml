@model eShopSolution.Web.Models.PromoteEmployeeViewModel
<input type="hidden" id="UserId" name="UserId" asp-for="UserId" />
<div class="form-group row">
    <label for="Name" class="col-sm-2 col-form-label">Tên: </label>
    <div class="col-sm-10">
        <input type="text" id="UserName" name="UserName" asp-for="UserName" class="form-control"  />
    </div>
</div>
<div class="form-group row">
    <label for="Email" class="col-sm-2 col-form-label">Email: </label>
    <div class="col-sm-10">
        <input type="Email" id="Email" name="Email" asp-for="Email" class="form-control"  />
    </div>
</div>
<div class="form-group row">
    <label for="PhoneNumber" class="col-sm-2 col-form-label">Số điện thoại: </label>
    <div class="col-sm-10">
        <input type="number" id="PhoneNumber" name="PhoneNumber" asp-for="PhoneNumber" class="form-control" required />
    </div>
</div>
<div class="form-group row">
    <label for="Name" class="col-sm-2 col-form-label">Chọn role: </label>
    <div class="col-sm-10">
        <select id="RoleDropdown"  class="form-control">
            <option value="">-- Chọn Role --</option>
            @foreach (var role in Model.Roles)
            {
                <option value="@role.RoleId">@role.RoleName</option>
            }
        </select>
    </div>
</div>
<div class="form-group row">
    <div class="col-sm-10 offset-sm-2">
        <button type="button" class="btn btn-success" id="submitEditUser">Cập nhật</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Đóng</button>
    </div>
</div>